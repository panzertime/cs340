package server.main;

public class ServerCommunicator {
	
	private int SERVER_PORT_NUMBER;
	private int MAX_WAITING_CONNECTIONS;
	
	/**
	 * Creates a server at the specified port number with the given number
	 * of max waiting connections and creates appropriate contexts
	 * @pre none
	 * @post the server will continually execute requests handed to it
	 */
	public void run() {
		
	}
	
	/**
	 * Halts the current running server.
	 * @pre Server is currently running
	 * @post the server will be stopped
	 */
	public void stop() {
		
	}
	
	public static void main(String[] args) {
		
	}
}
